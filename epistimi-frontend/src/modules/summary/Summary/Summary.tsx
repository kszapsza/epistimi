import { Spinner } from '../../../shared';
import './Summary.scss';

export const Summary = (): JSX.Element => {
  return (
    <div className="summary">
      ...it works!
      <Spinner/>
    </div>
  )
};
